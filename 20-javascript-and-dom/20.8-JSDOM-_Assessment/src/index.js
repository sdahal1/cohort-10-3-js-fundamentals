////////////////////////////
// DO NOT EDIT THIS ARRAY //
////////////////////////////
/*
 The contacts array contain the list of contacts for the contact book.
*/
window.contacts = [
  {
    id: 1,
    name: "Leanne Graham",
    username: "Bret",
    email: "Sincere@april.biz",
    picture: "https://via.placeholder.com/150/008600/FFFFFF?text=LG",
    address: {
      street: "Kulas Light",
      suite: "Apt. 556",
      city: "Green Vale",
      zipcode: "92998-3874",
      geo: {
        lat: "-37.3159",
        lng: "81.1496",
      },
    },
    phone: "1-770-736-8031 x56442",
    website: "hildegard.org",
    company: {
      name: "Romaguera-Crona",
      catchPhrase: "Multi-layered client-server neural-net",
      bs: "harness real-time e-markets",
    },
  },
  {
    id: 2,
    name: "Ervin Howell",
    email: "Shanna@melissa.tv",
    picture: "https://via.placeholder.com/150/0000FF/FFFFFF?text=EH",
    address: {
      street: "Victor Plains",
      suite: "Suite 879",
      city: "Blue Vale",
      zipcode: "90566-7771",
      geo: {
        lat: "-43.9509",
        lng: "-34.4618",
      },
    },
    phone: "010-692-6593 x09125",
    website: "anastasia.net",
    company: {
      name: "Deckow-Crist",
      catchPhrase: "Proactive didactic contingency",
      bs: "synergize scalable supply-chains",
    },
  },
  {
    id: 3,
    name: "Clementine Bauch",
    email: "Nathan@yesenia.net",
    picture: "https://via.placeholder.com/150/34F500/FFFFFF?text=CB",
    address: {
      street: "Douglas Extension",
      suite: "Suite 847",
      city: "Green Vale",
      zipcode: "59590-4157",
      geo: {
        lat: "-68.6102",
        lng: "-47.0653",
      },
    },
    phone: "1-463-123-4447",
    website: "ramiro.info",
    company: {
      name: "Romaguera-Jacobson",
      catchPhrase: "Face to face bifurcated interface",
      bs: "e-enable strategic applications",
    },
  },
  {
    id: 4,
    name: "Patricia Lebsack",
    email: "Julianne.OConner@kory.org",
    picture: "https://via.placeholder.com/150/008600/FFFFFF?text=PL",
    address: {
      street: "Hoeger Mall",
      suite: "Apt. 692",
      city: "South Vale",
      zipcode: "53919-4257",
      geo: {
        lat: "29.4572",
        lng: "-164.2990",
      },
    },
    phone: "493-170-9623 x156",
    website: "kale.biz",
    company: {
      name: "Robel-Corkery",
      catchPhrase: "Multi-tiered zero tolerance productivity",
      bs: "transition cutting-edge web services",
    },
  },
  {
    id: 5,
    name: "Chelsey Dietrich",
    email: "Lucio_Hettinger@annie.ca",
    picture: "https://via.placeholder.com/150/FF00FF/FFFFFF?text=CD",
    address: {
      street: "Skiles Walks",
      suite: "Suite 351",
      city: "North Vale",
      zipcode: "33263",
      geo: {
        lat: "-31.8129",
        lng: "62.5342",
      },
    },
    phone: "(254)954-1289",
    website: "demarco.info",
    company: {
      name: "Keebler LLC",
      catchPhrase: "User-centric fault-tolerant solution",
      bs: "revolutionize end-to-end systems",
    },
  },
  {
    id: 6,
    name: "Mrs. Dennis Schulist",
    email: "Karley_Dach@jasper.info",
    picture: "https://via.placeholder.com/150/000086/FFFFFF?text=DS",
    address: {
      street: "Norberto Crossing",
      suite: "Apt. 950",
      city: "South Vale",
      zipcode: "23505-1337",
      geo: {
        lat: "-71.4197",
        lng: "71.7478",
      },
    },
    phone: "1-477-935-8478 x6430",
    website: "ola.org",
    company: {
      name: "Considine-Lockman",
      catchPhrase: "Synchronised bottom-line interface",
      bs: "e-enable innovative applications",
    },
  },
  {
    id: 7,
    name: "Kurtis Weissnat",
    email: "Telly.Hoeger@billy.biz",
    picture: "https://via.placeholder.com/150/860000/FFFFFF?text=KW",
    address: {
      street: "Rex Trail",
      suite: "Suite 280",
      city: "Blue Vale",
      zipcode: "58804-1099",
      geo: {
        lat: "24.8918",
        lng: "21.8984",
      },
    },
    phone: "210.067.6132",
    website: "elvis.io",
    company: {
      name: "Johns Group",
      catchPhrase: "Configurable multimedia task-force",
      bs: "generate enterprise e-tailers",
    },
  },
  {
    id: 8,
    name: "Nicholas Runolfsdottir V",
    email: "Sherwood@rosamond.me",
    picture: "https://via.placeholder.com/150/FF0000/FFFFFF?text=NR",
    address: {
      street: "Ellsworth Summit",
      suite: "Suite 729",
      city: "Green Vale",
      zipcode: "45169",
      geo: {
        lat: "-14.3990",
        lng: "-120.7677",
      },
    },
    phone: "586.493.6943 x140",
    website: "jacynthe.com",
    company: {
      name: "Abernathy Group",
      catchPhrase: "Implemented secondary concept",
      bs: "e-enable extensible e-tailers",
    },
  },
  {
    id: 9,
    name: "Glenna Reichert",
    email: "Chaim_McDermott@dana.io",
    picture: "https://via.placeholder.com/150/226688/FFFFFF?text=GR",
    address: {
      street: "Dayna Park",
      suite: "Suite 449",
      city: "White Vale",
      zipcode: "76495-3109",
      geo: {
        lat: "24.6463",
        lng: "-168.8889",
      },
    },
    phone: "(775)976-6794 x41206",
    website: "conrad.com",
    company: {
      name: "Yost and Sons",
      catchPhrase: "Switchable contextually-based project",
      bs: "aggregate real-time technologies",
    },
  },
  {
    id: 10,
    name: "Clementina DuBuque",
    email: "Rey.Padberg@karina.biz",
    picture: "https://via.placeholder.com/150/000000/FFFFFF?text=CD",
    address: {
      street: "Kattie Turnpike",
      suite: "Suite 198",
      city: "Blue Vale",
      zipcode: "31428-2261",
      geo: {
        lat: "-38.2386",
        lng: "57.2232",
      },
    },
    phone: "024-648-3804",
    website: "ambrose.net",
    company: {
      name: "Hoeger LLC",
      catchPhrase: "Centralized empowering task-force",
      bs: "target end-to-end models",
    },
  },
];

///////////////////////////////////
// WRITE YOUR SOLUTION CODE HERE //
///////////////////////////////////

/*
 Create and return the HTML to render a single contact card.
 The `contact` parameter is an object representing a single contact. 
*/
function renderContact(contact) {
  let content = `
  <div class="card" data-id="${contact.id}">
    <button class="deleteBtn" title="Delete this contact">X</button>
    <div class="avatar">
      <div class="circle"></div>
      <div class="circle"></div>
      <img src= ${contact.picture}/>
    </div>
    <div class="info">
      <span class="name big">${contact.name}</span>
      <span class="email small">${contact.email}</span>
    </div>
    <div class="details">
      <div class="phone">${contact.phone}</div>
      <div class="website">${contact.website}</div>
    </div>

    <div class="additional">
      <div class="address">
        <div class="suite">${contact.address.suite}</div>
        <div class="street">${contact.address.street}</div>
        <div class="city">${contact.address.city}, ${contact.address.zipcode}</div>
      </div>
      <div class="company">
        <div class="label">Works at</div>
        <div class="company-name">${contact.company.name}</div>
      </div>
    </div>
  </div>
  `
  return content;
}

/*
  Render the array of contacts and insert them on the DOM.
  The contacts should be rendered in the `section` with id "contacts".
*/
function render(contacts=[]) {
  //select the section where I will add each contact to
  let section = document.querySelector("section#contacts");
  section.innerHTML="";
  console.log(section)
  contacts.forEach(contactObj=>{
    let content = renderContact(contactObj);
    section.innerHTML += content;
  })

  // loadCities(contacts);
  // filterHandler()
}

/*
  Filter by city. Filter the  array of contacts by the given city.
  Return a new array containing the filtered list. 
  Do NOT modify the original array.
*/
function filterByCity(city) {
  return window.contacts.filter(contactObj=>contactObj.address.city===city);
}

/*
  Add an `change` event listener to the `filterOptions` select element.
  On `change` get the value selected by the user. 
  If the value is "0" call `render()` with the complete contacts list.
  If the value is not "0" call `filterByCity()` passing the value selected by
  the user. Then call `render()` with the filtered list.
*/
function filterHandler() {
  let selectElement = document.querySelector("#filterOptions");
  //add event listener to select dropdown element to listen for a "change"
  selectElement.addEventListener("change",(event)=>{
    //the value from the dropdown in a variable
    let selectedOptionValue = event.target.value
    // If the value is "0" call `render()` with the complete contacts list.
    console.log("changing the select!", selectedOptionValue)
    if(selectedOptionValue === '0'){
      render(contacts);
    }else{
      //get a list of contacts who are from the selected city
      let filteredContacts = filterByCity(selectedOptionValue)
      console.log(filteredContacts)
      //re render the page with the new list of filtered contacts
      render(filteredContacts);
    }
  })
}

/*
  Accepts an array of contacts.
  Populate the select with id `filterOptions` with the list of cities.
  Create a list of cities from the contacts array with no duplicates then
  add an `<option>` element for each city to the select.
*/
function loadCities(contacts) {
  //from contacts, go through each contact and get the city of each contact into an array
  // let cities = window.contacts.map(contactObj=>contactObj.address.city) 

  // //use the new Set() class to get only the unique values
  // let unique = new Set(cities).values()

  // //convert the unique city set to an array
  // let uniqueCitiesArray = Array.from(unique);



  // //select the dropdown so we can append option elements into it later
  // let selectTag = document.querySelector("#filterOptions");


  // //for each city string in the uniqueCitiesArray, give back <option> element
  // let optionElements = uniqueCitiesArray.map((city,index)=>{
  //     return `<option value="${city}">${city}</option>`
  // })

  // optionElements.unshift(`<option value="0">-- Select a city --</option>`);


  // // console.log(optionElements.join(""))
  // selectTag.innerHTML = optionElements.join("")

  // const select = document.querySelector("#filterOptions");

  // const citiesOptions = uniqueCitiesArray.map((city) => `<option value="${city}">${city}</option>`);
  
  // citiesOptions.unshift('<option value="0">-- Select a city --</option>');
  // select.innerHTML = citiesOptions.join("");


  //get all the cities of all teh contacts into an array, it may have some duplicates though
  let citieswithDupes = contacts.map((contact) => contact.address.city)

  //remove duplicates from array using the new Set(insertArrayhere)
  let unique = new Set(citieswithDupes).values()

  //convert the unique set into an array
  let citiesNoDupes = Array.from(unique)

  //map through teh array containing no duplicates, and return an array of html code representing option tags (cities is an array of option tags writtten as strings)
  const cities = citiesNoDupes.map((city) => `<option value="${city}">${city}</option>`);

  //add the default option of "select a city" to the beginning of the cities array of options
  cities.unshift('<option value="0">-- Select a city --</option>');
  //select the drop down we want to insert the option tags into
  const select = document.querySelector("#filterOptions");

  //combine the option tags from the array into one string using .join(), and insert that string as html string to the select tag
  select.innerHTML = cities.join("");
}

/*
  Remove the contact from the contact list with the given id.
*/
function deleteContact(id) {
  const indexOfObject = window.contacts.findIndex(object => {
    return object.id === id;
  });
  if(indexOfObject === -1){ //this means that the id was not found
    return;
  }
  window.contacts.splice(indexOfObject, 1);
  console.log(window.contacts);
}

/*
  Add a `click` event handler to the `deleteBtn` elements.
  When clicked, get the id of the card that was clicked from the 
  corresponding `data-id` then call `deleteContact()` and re-render 
  the list.
*/
function deleteButtonHandler() {

  //select the container containing all the contacts and add an event handler for "click" event to it
  let contactsSection = document.querySelector("#contacts");
  contactsSection.addEventListener("click", (event)=>{
    console.log(event.target.nodeName)
    if(event.target.nodeName === "BUTTON"){
      let parentDiv = event.target.parentElement;
      //get the id of that parent element div
      let id = parentDiv.getAttribute("data-id");
      console.log("id is", id)

      deleteContact(parseInt(id));
      render(contacts);
    }
  })

  // //get all the delete buttons
  // let allDeleteButtons = document.querySelectorAll(".deleteBtn");
  // console.log(allDeleteButtons)
  // allDeleteButtons.forEach(button=>{
  //   button.addEventListener("click", (event)=>{
  //     //event.target represents the thing that was clicked
  //     let parentDiv = event.target.parentElement;
  //     //get the id of that parent element div
  //     let id = parentDiv.getAttribute("data-id");
  //     console.log("id is", id)

  //     deleteContact(parseInt(id));
  //     render(contacts);
  //   })
  // })

}

/*
  Perform all startup tasks here. Use this function to attach the 
  required event listeners, call loadCities() then call render().
*/
function main() {
  deleteButtonHandler();
  filterHandler();
  loadCities(window.contacts)
  render(window.contacts)
}

window.addEventListener("DOMContentLoaded", main);

//////////////////////////////////////////
// DO NOT EDIT ANYTHING BELOW THIS LINE //
//////////////////////////////////////////
window.contacts = contacts;
window.renderContact = renderContact;
window.render = render;
window.filterByCity = filterByCity;
window.filterHandler = filterHandler;
window.loadCities = loadCities;
window.deleteContact = deleteContact;
window.deleteButtonHandler = deleteButtonHandler;
window.main = main;
